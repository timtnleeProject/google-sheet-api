{
  "name": "restapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js --exec babel-node index.js",
    "build": "babel src -d build",
    "prod": "babel src -d build && nodemon index.js production",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "timtnlee",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-root-import": "^6.0.0",
    "babel-preset-es2015": "^6.24.1",
    "nodemon": "^1.17.5"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "googleapis": "^32.0.0"
  }
}
